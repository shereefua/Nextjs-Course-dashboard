import { Inter, Lusitana } from "next/font/google";

export const inter = Inter({ subsets: ["latin"] }); //check subset frm gogle (saved in bookmark> front en)

export const interGreek = Inter({ subsets: ["greek"] }); //check subset frm gogle (saved in bookmark> front en)


export const lusitana = Lusitana({
    weight: ["400", "700"],
    subsets: ["latin"],
});